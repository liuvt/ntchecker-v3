@page "/checkers/{userId}/{date}"
@layout MainLayoutEmpty

<HeadContent>
    <meta name="robots" content="noindex, nofollow" />
</HeadContent>
<SeoHeaderComponent Title="Lịch sử phiếu Checker v3.1 - Taxi Nam Thắng"
                    Description="@($"Xem lại phiếu Checker tài xế {userId}, ngày {date}")"
                    Image="imgs/customers/homepage.jpg" />

<CheckerDetailHistoryComponent userId="@userId" date="@date" @rendermode="new InteractiveWebAssemblyRenderMode(prerender: true)" />

@code {
    [Parameter]
    public string userId { get; set; } = string.Empty;
    [Parameter]
    public string date { get; set; } = string.Empty;
}

@page "/checkers/{userId}"
@layout MainLayoutEmpty

<HeadContent>
    <meta name="robots" content="noindex, nofollow" />
</HeadContent>

<CheckerDetailComponent userId="@userId" @rendermode="new InteractiveWebAssemblyRenderMode(prerender: true)" />

@code {
    [Parameter]
    public string userId { get; set; } = string.Empty;
}

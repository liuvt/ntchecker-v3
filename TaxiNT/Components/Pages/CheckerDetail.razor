@page "/checkers/{userId}"
@layout MainLayoutEmpty

<HeadContent>
    <meta name="robots" content="noindex, nofollow" />
</HeadContent>
<SeoHeaderComponent Title="Phiếu Checker v3.1 - Taxi Nam Thắng"
                    Description="@($"Kiểm tra phiếu Checker tài xế {userId}, đối soát lịch sử chạy xe ngày hôm qua")"
                    Image="imgs/customers/homepage.jpg" />

<CheckerDetailComponent userId="@userId" @rendermode="new InteractiveWebAssemblyRenderMode(prerender: true)" />

@code {
    [Parameter]
    public string userId { get; set; } = string.Empty;
}

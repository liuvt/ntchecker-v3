@inherits LayoutComponentBase

<div class="flex h-screen flex-col">
    <!-- Nội dung chính -->
    <div class="flex-1 overflow-auto p-4 pb-20">
        <!-- Thêm pb-20 để chừa chỗ cho thanh dưới -->
        @Body
    </div>
</div>

<!-- Bottom Tab Navigation -->
<div class="shadow-t fixed bottom-0 left-0 z-50 flex w-full justify-around border-t bg-white p-2">
    <NavLink href="/" class="text-center" activeClass="font-bold text-blue-500">
        <div>🏠<div class="text-xs">Home</div></div>
    </NavLink>
    <NavLink href="/checkerdetail" class="text-center" activeClass="font-bold text-blue-500">
        <div>🏠<div class="text-xs">Phiếu Checker</div></div>
    </NavLink>
    <NavLink href="/checker" class="text-center" activeClass="font-bold text-blue-500">
        <div>✅<div class="text-xs">Lịch sữ</div></div>
    </NavLink>
    <NavLink href="/account" class="text-center" activeClass="font-bold text-blue-500">
        <div>👤<div class="text-xs">Tài khoản</div></div>
    </NavLink>
</div>

